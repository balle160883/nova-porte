version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "4000:4000"
    restart: always
    environment:
      - PORT=4000
      - SUPABASE_URL=https://xygarchwyrflpzywcpid.supabase.co
      - SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inh5Z2FyY2h3eXJmbHB6eXdjcGlkIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3MzE3MzU5NywiZXhwIjoyMDg4NzQ5NTk3fQ.NhK0bVSyLcWAP8EXU35agSs89DCq2LBhRTXv2_P-Y0A
      - JWT_SECRET=cobranza-pro-secret-2026

  frontend:
    build:
      context: ./frontend
      args:
        # URL interna del backend en la red Docker (usada por Next.js server-side rewrite proxy)
        # El browser nunca ve esta URL; solo llama a /api-backend/* (same-origin HTTPS)
        - BACKEND_URL=http://backend:4000
    ports:
      - "3000:3000"
    restart: always
    environment:
      - BACKEND_URL=http://backend:4000
    depends_on:
      - backend
